name: First Automation

on:
  push:
    branches:
      - main

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "GitHub Actions Working Successfully"
